{
  "name": "clicker-generator",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Генератор кликеров: превращает 3D-модель в разъёмный корпус с полостью под механический щёлкающий механизм",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "tsc --noEmit && vite build --ssr test/pipeline.test.ts --outDir .test-build --logLevel warn && node .test-build/pipeline.test.js"
  },
  "dependencies": {
    "jszip": "^3.10.1",
    "three": "^0.185.1",
    "three-bvh-csg": "^0.0.18",
    "three-mesh-bvh": "^0.9.14"
  },
  "devDependencies": {
    "@types/three": "^0.185.0",
    "typescript": "^5.9.3",
    "vite": "^8.2.0"
  }
}
